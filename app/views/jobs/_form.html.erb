<%= form_for(@job) do |f| %>
  <% if @job.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@job.errors.count, "error") %> prohibited this job from being saved:</h2>

      <ul>
      <% @job.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field mdl-textfield mdl-js-textfield">
    <%= f.label :name, class: "mdl-textfield__label" %>
    <%= f.text_field :name, class: "mdl-textfield__input" %>
  </div>
  <br>
  <div class="field mdl-textfield mdl-js-textfield">
    <%= f.label :description, class: "mdl-textfield__label" %>
    <%= f.text_area :description, class: "mdl-textfield__input" %>
  </div>
  <div class="actions">
    <%= f.submit "Submit", class: "mdl-button mdl-js-button mdl-button--raised mdl-button--colored" %>
    <%= link_to 'Back', jobs_path, class: "mdl-button mdl-js-button mdl-button--raised" %>
  </div>
<% end %>
